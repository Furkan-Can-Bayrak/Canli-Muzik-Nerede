#!/bin/sh
# Cursor'ın commit mesajına eklediği Co-authored-by satırını kaldırır.
MSG_FILE="$1"
[ -f "$MSG_FILE" ] || exit 0

tmp="${MSG_FILE}.tmp"
grep -viE 'co-authored-by:.*cursor|co-authored-by:.*cursoragent@cursor\.com' "$MSG_FILE" > "$tmp" || true
mv "$tmp" "$MSG_FILE"
